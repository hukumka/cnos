#include <io.h>
#include <memory.h>

#define LogPrintf printf

uint8* LoadVolumeIdSector( uint32 volumeId_sector_id ){
	uint8* volumeId_sector = (uint8*)Allocate(1);
	if( volumeId_sector_id == NULL ){
	}
}
bool InitFat32( uint32 volumeId_sector_id ){
	uint8* volumeId_sector = (uint8*)Allocate( 1 );
}
